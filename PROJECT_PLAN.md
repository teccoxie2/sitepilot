# SitePilot 90 天执行计划

最后更新：2026-08-14
适用对象：sitepilot.co 生产站点

## 目标与边界

SitePilot 的产品定位是**以证据为核心的数字基础设施采购决策系统**，而不是继续扩张的泛 SEO/联盟媒体站。

核心产品闭环：

```text
类别发现 → 方法论与证据 → 交互式评估 → 可交付决策报告 → 审计/定制评估/合适的联盟推荐
```

当前基线：生产站可访问，sitemap 约 140 个 URL，已有 `/methodology`、多个 ROI/成本/风险/采购工具和 `/apply-for-audit`。未来 90 天冻结泛化内容扩张；只做能进入上述闭环的清理、证据、工具和转化工作。

## 两个主航道

### 主航道 A：AI 企业采购与实施

服务技术负责人、运营负责人和采购团队，覆盖从供应商发现到预算、风险、短名单、pilot 和实施的连续决策路径。

- 类别 hub：`/ai-tools`、`/tools/evaluation-tools-2026`
- 证据/比较：供应商比较、尽调、成本效益、风险与安全合规页面
- 操作资产：vendor shortlist / procurement matrix、实施成本与 ROI、风险清单、RFP/pilot 模板
- 主转化：工具完成 → 决策报告导出/分享 → `/apply-for-audit`

### 主航道 B：Web 基础设施选型

把 hosting、website builder 和 domain 组织为网站基础设施决策线，服务需要上线、迁移或降低运营风险的团队。

- 类别 hub：`/hosting`、`/website-builders`、域名购买入口
- 证据/比较：主机、建站平台、WordPress 和域名选择的测试与来源
- 操作资产：hosting/platform fit scorecard、Website Builder vs WordPress 决策工具、迁移与安全清单
- 主转化：适配评分 → 可分享选择摘要 → 审计/推荐

代理基础设施保留为独立垂直入口，90 天内只维护事实、合规边界和既有工具，不在首页与两个主航道中等权扩张。

## 页面处置：Keep / Merge / Noindex / Remove

在第 1–14 天为 sitemap 中全部约 140 个 URL 建立清单，每个 URL 只能有一个处置状态，并记录目标页面、证据依据、负责人和复核日期。

| 状态 | 使用条件 | 必须动作 | 验收标准 |
|---|---|---|---|
| **Keep** | 属于两个主航道、代理独立垂直或必要的信任/工具入口；有独立搜索意图和证据 | 保留并接入 hub → 工具/证据 → 报告路径 | 200、可索引、canonical 自指、至少一个相关内容页入链、核心 claim 可追溯 |
| **Merge** | 同义词、年份、轻微意图差异或重复模板，没有独立证据/决策结果 | 选一个主 URL 合并内容和内链；旧页面从代码和 sitemap 中移除 | 主页面覆盖完整意图；不保留重复内容或“兼容层” |
| **Noindex** | 暂时需要访问但不应参与搜索，例如低价值工具壳、内部/实验结果页 | 加 `noindex,follow`，从 sitemap 移除；保留必要的站内入口 | 抓取检查确认 noindex 生效且不出现在 sitemap |
| **Remove** | 404、无维护能力、没有独立价值、法律/隐私上不应公开或无法提供可信证据 | 直接删除页面、内链和 sitemap 条目 | 生产路由不再输出页面；没有残留内链 |

第一轮优先检查：重复“Best X”变体、只有数字没有来源的页面、孤立工具、与 hub 无法形成决策路径的文章。处置完成前不得新增泛化关键词页。

## Claim ledger（量化主张台账）

所有重要页面和工具共用一个版本化 claim ledger（先用静态 JSON/CSV 即可，不引入数据库）。每条高确定性数字必须记录：

当前机器可校验 ledger：`src/data/evidence/sitepilot-claim-ledger-v1.json`；编辑摘要见 `CONTENT_CLAIM_LEDGER.md`，由 `npm run claims:ledger` 校验。

```text
claim_id
页面/组件
原文主张
数值与单位
来源 URL 或原始数据位置
样本量与样本定义
采集/测试日期
计算公式或推导步骤
结论类型：实测 / 供应商声明 / 公开资料 / 编辑判断
适用条件与不确定性
负责人
下次复核日期
状态：verified / estimate / example / remove
```

没有来源、样本、日期和公式的“500+ companies”“245% ROI”等主张必须改成明确的估算/示例/情景假设，或直接删除。页面应显示方法论、证据类型和检查日期；不允许用虚构作者、评分、日期或来源补齐 schema。

## 核心工具闭环

90 天内优先完成以下 3 个工具，不再平行建设大量一次性计算器：

1. **AI vendor shortlist / procurement decision matrix**：比较 workflow fit、成本、安全、支持、实施证据，输出可解释的 shortlist。
2. **AI implementation cost + ROI**：公开输入假设、成本项、回收期和敏感性，输出预算/ROI 摘要。
3. **Hosting/platform fit scorecard**：按站点类型、流量、迁移成本、SEO 控制、支持和长期持有成本给出适配结果。

每个工具都必须具备：

- 输入项、默认值和假设说明；
- 可复核的评分/公式和结果解释；
- 一句话决策摘要与下一步建议；
- 可复制的结果 URL 或报告 ID；
- PDF/CSV/决策备忘录导出中的至少一种；
- “申请审计/获取定制评估”的明确 CTA；
- `tool_start`、`tool_complete`、`report_export`、`report_share`、`apply_submit`、`affiliate_click` 事件。

登录、数据库、复杂权限和完整 SaaS 工作区必须等工具完成率、分享率和导出率已有稳定证据后再评估。

## 90 天节奏

### 第 1–14 天：收敛与清理

- 冻结新增泛化文章和新的“Best X 2026”变体。
- 完成约 140 个 URL 的 Keep / Merge / Noindex / Remove 清单。
- 建立 claim ledger，先覆盖首页、两个主航道 hub、比较页和 3 个核心工具。
- 清理孤立页、重复 H1、异常 slug、失效 OG、重复 metadata 和无关内链。
- 接入并核对 Search Console、分析工具和转化事件；确认生产 sitemap 与源码一致。

### 第 15–30 天：证据与数据层

- 为核心供应商/平台建立统一事实记录：价格、性能、可靠性、安全、迁移成本、锁定风险、支持质量。
- 每条记录带来源 URL、来源日期、检查日期、结论类型、适用条件和复核日期。
- 发布方法论说明和证据类型标签；为主要页面补齐作者/审阅者、检查日期和假设区块。
- 将同一份事实数据接入比较页和工具，避免文章、评分器和 CTA 使用互相矛盾的数字。

### 第 31–60 天：第一个可交付工具产品

- 完成 AI vendor shortlist 工具的输入 → 评分 → 结果 → 导出 → 审计申请闭环。
- 为 AI implementation cost + ROI 和 hosting/platform fit scorecard 复用已验证的结果/事件规范。
- 核对分享 URL、导出文件、事件参数和 CTA 归因；用真实任务做端到端验收。
- 发布一份有版本、有来源、有方法的小型 AI 采购 benchmark，不以新增文章数量作为进度指标。

### 第 61–90 天：验证与取舍

- 将两个主航道的 hub → 证据页 → 工具 → 报告 → 审计申请串成完整链接路径。
- 补齐工具结果页的可解释性、下一步建议和适用边界；替换未验证的高 ROI 叙事。
- 发布更新日志，记录事实变更、评分变化和页面处置变化。
- 以本计划的指标和验证门槛做 90 天复盘：扩大有效路径，合并/不索引/删除无效路径。

## 指标体系

### 北极星指标

**每月完成的合格决策输出数**：完成工具报告、导出/分享报告或提交审计申请，且结果包含有效输入和可追溯版本。

### 产品与商业指标

- 工具启动数、完成数、完成率；
- 报告导出率、分享率、复访率；
- `apply_submit` 数、合格线索率、审计成交率；
- 每 1,000 次非品牌自然访问带来的合格输出和联盟收入；
- CTA 到报告、报告到申请的转化率。

### SEO 与内容质量指标

- 两个主航道的非品牌点击、展示、CTR 和前 20 名页面数；
- 已索引 URL 占比、sitemap 处理状态和抓取异常；
- 重要量化 claim 的来源覆盖率；
- 内容更新时间中位数和证据复核逾期率；
- Keep / Merge / Noindex / Remove 的月度变化。

不把 pageviews、文章数量或联盟点击单独当作成功标准。

## 验证门槛

### 页面与索引门槛

- sitemap 每个 URL 都是 200、可索引、canonical 自指，并有至少一个相关内容页入链；
- 每个内容页只有一个 H1；title/description 经过长度和 SERP 展示复核；
- 可分享页面的 OG/Twitter 图片返回 200；
- Keep 页面有证据入口，Merge/Noindex/Remove 页面没有残留的错误内链。

### 证据门槛

- 核心页面的量化 claim 100% 有 ledger 记录；
- 没有来源的数字必须标记为 estimate/example 或删除；
- 每次评分或推荐都能回溯到版本、输入、公式和检查日期；
- 不以虚构作者、评分、来源或日期填充结构化数据。

### 工具门槛

- 真实用户可完成输入、获得解释性结果并导出/分享；
- 事件从 `tool_start` 到 `apply_submit` 能在分析系统中串联；
- 结果页有明确下一步和适用边界；
- 若启动多但完成少，先改输入和交互，不扩内容；若完成多但导出/申请少，先改结果页和 CTA。

### 90 天决策门槛

- 自然流量增长但无工具行为：停止扩张该关键词簇，重做搜索意图和页面路径；
- 某页面/垂直线连续 90 天没有合格输出：降级为维护，按 Merge / Noindex / Remove 处理，不再新增同类页面；
- 只有当工具报告和审计线索稳定后，才评估付费 benchmark、团队工作区、提醒和 API。

## 工作节奏

- 每周一次检查指标、证据逾期和页面处置状态；
- 每次 Review 队列变更先运行 `npm run content:review-signals -- --verify`，将源码信号与 Search Console/线索证据分开记录；
- 每次发布必须附验证结果和影响范围；
- 所有新增工作都要指向两个主航道、claim ledger 或核心工具闭环；
- 优先修复真实用户路径，不以“更多页面”替代产品证据。

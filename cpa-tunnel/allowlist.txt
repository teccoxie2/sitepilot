# 每行一个 IP 或 CIDR。写了生效行之后，只放行这些来源（读取 Cloudflare 的 Cf-Connecting-IP）。
# 只有注释或空文件 = 不按 IP 过滤；仍必须带 X-CPA-Gate，且只允许 /v1。
# Vercel 函数出口 IP 不固定，生产请靠门禁令牌，不要把未知 IP 写进白名单冒充 Vercel。
# 本机探测可加 127.0.0.1
#
# 203.0.113.10
# 2001:db8::/32

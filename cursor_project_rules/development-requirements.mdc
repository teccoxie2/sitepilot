---
description: 奥克兰住宅开发核算台的开发硬性要求（知识库）。
---

# 开发要求

1. **不允许使用假数据。** 地址、地块、规划、地形、价格必须来自可公开核对的来源。禁止 mock、编造、占位数字、lorem，以及用大模型定价或补全地块事实。公开航拍不是直播；未配置视觉模型时不得编造看见的内容。正式 LIM 只认客户上传的议会 PDF 文字层；公开洪水图层不能冒充 LIM。V1 图纸物料验证可用大模型读文字层，但金额只走价库，数量由服务器重算。V2 Estimator 允许对无文字层图纸走 OCR/Vision，提取必须带页码/bbox/confidence；Vision 读数不得标成 `VERIFIED`，除非与文字层或表单元格一致；数量无公式则 `INFERRED`/`UNPRICED`/`UNRESOLVED`；禁止 `MODEL` 费率。
2. 外部数据读不到时：返回错误、空结果或 `status: "missing"`。不要用默认地块、平均价或上次结果冒充本次读取。
3. 价表条目必须有 `source_name`、`source_url`、`retrieved_at`。没有公开单价就不要进价表。
4. 屋主界面只展示真实读取与真实套价；缺项要看得见，不能藏成 0 元成交。
5. **V1 图纸与 LIM 只认 PDF 文字层。** 门窗表、GFA、层高、覆盖率必须带来源句；LIM 的污染、风区、径流和 LIR 必须来自上传文件正文。V1 扫描件无文字层则报错。禁止在 V1 用 YOLO/OCR 猜毫米或猜附图，禁止用户型模板面积冒充图纸面积。
6. **V2 数量状态。** `VERIFIED` | `CALCULATED` | `INFERRED` | `ALLOWANCE` | `UNPRICED` | `UNRESOLVED`。`CALCULATED` 必须有 formula 与 inputs。人工修正只追加 `correction_events`。

细则见仓库根目录 `.cursor/rules/no-fake-data.mdc` 与 `README.md` 的「开发要求」。

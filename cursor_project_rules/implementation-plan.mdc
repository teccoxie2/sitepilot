---
description: 功能开发步骤记录。每完成一步标注 Done 并写两行说明。
---

# implementation-plan

## 禁止假数据写入开发要求

Done

把「不允许使用假数据」写成仓库硬性约定：Cursor 规则、cursor_project_rules 知识库、README 开发要求。缺公开源必须标缺项或报错，不得用 mock 或大模型补数。

## 缺价科目重新检索公开报价源

Done

按 2026-08-24 网页标价补入 Kaboodle 橱柜/门板/台面、Expol Tuff Pod、Caroma 龙头、Crommelin 防水、H5 桩、土工布、Diy Supply 1800/1200 新铝窗、Hume 门扇、Metroscaff 移动脚手架周租、议会 RC 押金；无精确尺寸或无工时的科目仍缺项。

## 厨房电器水槽、卫生间水管、外围脚手架补公开源

Done

水槽/灶具包/厨房龙头按 Bunnings SKU 计价；卫生间预埋与 fit-off 按 Chambers 分项指引；外围脚手架按 SK Scaffold $18/m²+$1/m²/周（最低 1 周，另加 GST）。水槽安装与电器接线、洗衣房/热水器仍缺项。

## 第二阶段：RC/BC 图纸文字层套价

Done

项目页上传 RC/BC PDF，用 pypdf 读文字层（门窗表、GFA、底层/屋面、层高、覆盖率、砌块贴面/400mm 立柱、挡土墙高度），LangGraph 节点 parse_drawings → drawing_template → drawing_cost → drawing_explain 套同一价库。扫描件无文字层报错；无面积不套结构模板；无厨卫不套洁具；对不上的樘标缺项。仓库没有 115 Bruce 原图，不编造窗表。

## 拆分后门牌按同一 DP 合计面积核算图纸

Done

选 115A 这类单元门牌时，按地籍 DP 拉取同路同号的兄弟地块（Glenfield 115 Bruce 现址为 115A–F / DP 580591）。多套图纸覆盖率与能否放下用合计面积；户型模板仍按当前这一户。地址下拉在搜无字母的 115 时说明议会已无整宗门牌。

## 开发完成后只按议会现址筛选

Done

拆分门牌只展示当前这条 AC_Address / AC_Property 记录。地籍按门牌字母精确筛选；方案去掉联排/双拼等放不进本户的模板；图纸若大于本户地块则标与现址不符，不再把兄弟地块合计成整宗。

## 加密 RC/BC 读文字层失败修复

Done

补 cryptography 以读取 AES 加密图纸；按 `2100H x 860W ED 11` / Proposed Coverage / LOT FFL 抽取；BC 与 RC 地址不一致时不用错地的窗表。解析异常返回可读中文错误，不再只显示「图纸核算失败」。

## 关系模型、PriceProvider 与拆开的 LangGraph

Done

项目持久化为关系表（项目 / 地块快照 / 方案 / 成本版本 / 图纸集 / 价表版本），默认 SQLite，可用 `DATABASE_URL` 切 Postgres。单价只经 PriceProvider（价表优先、API 预留）。图改为 geocode→land→rules→typology→quantity→building_rules→cost→explain→pm_gate，checkpoint 落盘，`interrupt()` 留给后期 PM 面板。屋主页加地块快照、方案对比与成本树。

## 首页改为已查询项目下拉

Done

去掉「已建项目」长列表。同一地址只保留最近一次查询，收进地址卡片底部的下拉；项目页地块信息不再与快照重复罗列。

## 第一阶段土地数据后做航拍核对与更细方案

Done

`rules` 之后增加 `site_vision`：读取 Esri 最新公开航拍与 Wayback 历史镶嵌、LINZ 屋顶轮廓，可选把航拍送给视觉模型。模型与航拍都不能改地籍/区划/金额；hints 只调整方案排序。缺密钥或图层超时失败开放。项目页并排展示最新/历史图。

## 查询时核对 LIM 公开图层并计入订购费

Done

公开图层核对不是正式 LIM PDF。Howick 55 Nelson 的正式 LIM（2024-07-23）写明本户与 Overland Flow Path 相交、无监管污染记录，并可能含雨水管 LIR。公开核对改为用地块外包矩形查简化 OLFP 图层；门牌点会漏。全区滑坡 Low 不再当成 LIM 约束。每个方案另标正式 LIM 管网通知缺项，不编造 LIR 金额。

## 对照 55 Nelson 正式 LIM 补地面径流

Done

以 2024-07-23 正式 LIM 为结构参考（不把该 PDF 写进价库）：公开核对必须能读到 OLFP，管网 LIR/风区/许可标为正式 LIM 才有。Howick 地块外包矩形命中汇水 2000m²–1ha 路径，与 LIM 正文一致。

## 对照正式 LIM 分栏展示公开核对

Done

公开核对按 s44A 栏对齐：Site Contamination、Wind Zones、Soil Issues、Flooding、Overland Flow Path、Exposure Zones、Coastal、雨污管、许可、规划。Flooding 使用所有 LIM 都有的说明（图上无洪水不排除地面径流）。不把 55 Nelson 的风区、LIR 编号或许可清单套到其他地址。

## 正式 LIM 改为客户上传 PDF

Done

查询不再用公开图层冒充 LIM，也不把议会订购费计入造价。项目页上传议会 LIM PDF，只读文字层；地址必须与当前门牌及郊区一致。读到的地面径流和 LIR 只加缺项，不编单价。扫描件与附图不识别。

## 正式 LIM 按字段写开发影响，不贴原文

Done

栏目、建议卡和造价缺项改为中文开发影响：雨水管可能卡住继续加密、径流相交不是禁建、无污染监管数据不等于已清洁、低风区不改 E2 分值。LIR/许可英文原文不再进入界面。已上传项目在打开时按新规则刷新。

## 选装收进方案 Tab

Done

项目页「因地制宜初版方案」增加第三个 Tab「按需求选装」，表单不再单独挂在方案和图纸下面。起点仍是当前选中的方案卡片。

## MVP 前端与生产发布

Done

项目页加顶栏、章节锚点和方案 tab 的 URL 状态；核算轨迹默认折叠。补 Dockerfile 与生产启动脚本，便于公网部署。

## 清空查询记录，首页不再公开列出全库

Done

停服务后删除本机 SQLite、LIM、图纸和 LangGraph checkpoint。`GET /projects` 固定返回空列表；首页「本机最近查询」只读当前浏览器 localStorage，别人打开同一站点看不到你查过的地址。

## RC/BC 图纸物料验证页

Done

单独页面 `/drawing-takeoff` 上传 RC/BC PDF，只读文字层后按施工区域列出材料（厨房、卫生间、门窗、屋面等）。不写入项目库，不含法定费用。扫描件无文字层报错，不对不上的 SKU 编价。

## 图纸物料验证改为大模型推导

Done

`/drawing-takeoff` 调用大模型读 PDF 文字层：只接受能在原文对上的字段与门窗表，并选择价库 SKU。数量按公式或窗表由服务器重算，模型输出的单价丢弃。缺 `OPENAI_API_KEY` 或扫描件则失败。页面保留正则+公式对照。

## 图纸验证可走本地 CPA

Done

认 `CPA_BASE_URL` / `CPA_API_KEY`（管理页 URL 会收成 `/v1`）。就绪检查会探测 `/v1/models`；Chat 在 CPA 不支持 `json_object` 时自动降级。密钥不入库。

## 图纸验证读图过粗、科目缺失

Done

根因是封面截断文字、大模型结果未与正则全文合并、没有公式的材料被整行丢掉。改为优先送门窗表/面积页，合并正则与模型抽取，公式科目补全模型未点名的行，原文件数可计价，对不上的材料改为缺项而不是删除。

## 图纸图表逐行抽取与逐页 debug

Done

文字层按页用 layout + 字形坐标拼行，抽出门窗表/面积表/覆盖率每一行；无文字页标 `drawing_no_text` 且不做 OCR。送给模型时附上图表 JSON，接地核对应全文。验证页展示逐页字数、表的每一行和抽取核对数字。

## 图纸验证 Failed to fetch

Done

长耗时核对会堵住单线程事件循环，浏览器或隧道约 60 秒掐线后只显示 Failed to fetch。改为 POST 立即返回任务编号、后台线程读图，前端轮询进度；网络中断改为中文说明。

## 图纸验证大模型 read timeout

Done

送给模型的文字从 12 万字收到约 3.6 万，并优先保留门窗表页。读超时从 120 秒加到 210 秒，超时后自动缩到表页再试一次。错误改为中文，不再把 httpx 的 The read operation timed out 直接甩给页面。

## 对照 184 项住宅总账把未读出的工种列出来

Done

对照全包合同四段结构和一份 184 项奥克兰住宅逐行总账（金额不是价源）。补上桩基、钢构、内隔墙、电工分项、热泵、接驳等缺项。华夫板按 Firth 手册计入肋梁混凝土；天花 GIB 按 GFA 另计；屋面衬垫按 2026-08-30 Thermakraft 215 零售。三层及以上才列出电梯。成本树按科目嵌套。

## 公网链接改为常驻 Fly 源站

Done

Fly 应用 `vsense-auckland-dev-cost` 已 remote-only 部署到 Sydney，源站 `https://vsense-auckland-dev-cost.fly.dev`。Vercel 项目 `temporary-fast-rowan-7ti4bm3` 的网关改写指向该 fly.dev；公网入口保持 `https://demo-cost.vsense.co.nz`，不改 Microsoft 365 DNS nameserver。

## V2 Sprint 0：规则例外与 574 评测目录

Done

写入 V2 OCR/Vision 例外与六种数量状态；`evals/574-remuera/` 只放设计文档附录真值。原 PDF 未入库时 eval 报 `missing`，不编造图号。

## V2 Sprint 1：Document Intelligence

Done

`server/app/estimator/` 做 SHA256、PyMuPDF 预检、150DPI 渲染、文字层检测、页分类与 title block。无文字层且无视觉密钥时图号保持空，不编造。

## V2 Sprint 2：Completeness

Done

封面 Drawing Index 解析 expected vs supplied；缺失 sheet 进入 UNRESOLVED。交叉引用目标不在本套图内则标 Missing Document。

## V2 Sprint 3：Evidence 与图纸查看器

Done

Evidence 带 normalized bbox；`/estimator/[id]` DOCUMENTS Tab 叠框。人工改框只追加 MANUAL 证据与 correction_event，不覆盖原提取。

## V2 Sprint 4：Floor Area / Beam / Roof

Done

只从文字层证据生成这三类 takeoff。`CALCULATED` 屋面延米带 `roof_m2 / cover_width_m`。扫描件无提取则工程量为空。

## V2 Sprint 5：Scope 与确定性成本

Done

Scope 05/07 映射现有价表。Quote line 绑定 pricebook id 与图纸集版本。无单价的钢梁标 UNPRICED，不进确定总价。

## V2 Sprint 6–7：Review 与 Estimate UX

Done

Review Queue 分 AUTO_ACCEPTED / NEEDS_REVIEW / UNRESOLVED，Accept/Reject 只追加历史。Estimate Tab 展示 Expected、Range、Completeness 与 Not Included。

## V2 Sprint 8：574 回归 runner

Done

`GET /estimator/evals/574-remuera` 与 `run_574_eval()`：无原 PDF 则 documents_status=missing、passed=false。`unsupported_ai_generated_rate` 固定为 0。


# 总体顺序

版本：2026-09-12-a

1. **N00** 源码核实、coverage、任务契约、调度器短跑。
2. **N00-03** 诚实披露 Vision stub（真实小改动，验证调度器独立验收）。
3. **N01** 保护已有报价快照与指定版本导出（源码已实现，跑现有测试标 verified_existing）。
4. **N02** 原件与工作区保存恢复：本地 SQLAlchemy 已写共享库；生产库由 N08-01 重建。用户 2026-09-13 确认已认领。
5. **N03** 人工维护 Drawing Index（API + 工作区表单 + 项目隔离）。
6. **N04** 人工新增漏量（依赖 N00、N01；N03 不是硬依赖）。**已完成 API/单测。**
7. **N05** 修订识别最小切片（依赖 N03）。**已完成**：同图号不同修订可并存；取量只计入 CURRENT。
8. **N06** 已计价范围与正式发布边界（依赖 N01；角色体系未实现则拆开，不伪造批准）。
9. **N07** 集成检查与早晨报告。

生产 Prisma 重建 **N08-01** 已在 2026-09-12 续跑完成；用户 2026-09-13 确认已认领。不把 checkpoint 绑到 `DATABASE_URL`。

# 总体顺序

版本：2026-09-12-a

1. **N00** 源码核实、coverage、任务契约、调度器短跑。
2. **N00-03** 诚实披露 Vision stub（真实小改动，验证调度器独立验收）。
3. **N01** 保护已有报价快照与指定版本导出（源码已实现，跑现有测试标 verified_existing）。
4. **N02** 原件与工作区保存恢复：本地 SQLAlchemy 已写共享库；生产库过期且本环境不能写 Vercel 环境变量 → 记录阻塞，不挡代码任务。
5. **N03** 人工维护 Drawing Index（API + 工作区表单 + 项目隔离）。
6. **N04** 人工新增漏量（依赖 N00、N01；N03 不是硬依赖）。
7. **N05** 修订识别最小切片（依赖 N03）。
8. **N06** 已计价范围与正式发布边界（依赖 N01；角色体系未实现则拆开，不伪造批准）。
9. **N07** 集成检查与早晨报告。

生产 Prisma 重建是运维任务 **N08-01**，因 Vercel CLI 未登录而 BLOCKED。不因此停止 N00–N03、N01 等不依赖生产库的工作。
